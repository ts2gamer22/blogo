# Convex Deployment Configuration
# Get these values by running: npx convex dev
# Copy this file to .env.local and fill in your actual values

# Convex deployment ID (internal identifier)
CONVEX_DEPLOYMENT=dev:your-deployment-id-here

# Convex public URL (for WebSocket connection)
VITE_CONVEX_URL=https://your-deployment.convex.cloud

# Convex site URL (ends in .site, for Better Auth)
VITE_CONVEX_SITE_URL=https://your-deployment.convex.site

# Your local site URL
SITE_URL=http://localhost:3000

# GitHub OAuth (create app at: https://github.com/settings/developers)
GITHUB_CLIENT_ID=your_github_client_id_here
GITHUB_CLIENT_SECRET=your_github_client_secret_here

# Better Auth Secret (generate with: openssl rand -base64 32)
# Note: This should be set in Convex env, not .env.local
# Run: npx convex env set BETTER_AUTH_SECRET $(openssl rand -base64 32)
